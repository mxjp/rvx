{
	"type": "module",
	"scripts": {
		"start": "rolldown --config rolldown.config.js --watch",
		"build": "rolldown --config rolldown.config.js && tsc --noEmit"
	},
	"dependencies": {
		"rvx": "file:.."
	},
	"devDependencies": {
		"rolldown": "^1.1.4",
		"typescript": "^7.0.2"
	}
}
